!(function r(e, n, t) {
    function o(i, f) {
        if (!n[i]) {
            if (!e[i]) {
                var c = "function" == typeof require && require;
                if (!f && c) return c(i, !0);
                if (u) return u(i, !0);
                var a = new Error("Cannot find module '" + i + "'");
                throw ((a.code = "MODULE_NOT_FOUND"), a);
            }
            var p = (n[i] = { exports: {} });
            e[i][0].call(
                p.exports,
                function (r) {
                    return o(e[i][1][r] || r);
                },
                p,
                p.exports,
                r,
                e,
                n,
                t
            );
        }
        return n[i].exports;
    }
    for (var u = "function" == typeof require && require, i = 0; i < t.length; i++) o(t[i]);
    return o;
})(
    {
        1: [
            function (require, module, exports) {
                "use strict";
                var $;
                (window.anchorAnimateOffset = 0),
                    ($ = jQuery).extend($.fn, {
                        anchorAnimate: function () {
                            return this.each(function () {
                                $(this).bind("click.hashscroll", function (e) {
                                    if ($(this).attr("href")) {
                                        var targetHref = $(this).attr("href").split("#");
                                        if (targetHref.length > 1) {
                                            var docHref = document.location.href.split("#");
                                            if (targetHref[0] === docHref[0])
                                                if (targetHref[1]) {
                                                    var destination = $("#" + targetHref[1]);
                                                    destination &&
                                                        destination.length &&
                                                        (e.preventDefault(), $(".we-hashscroll-trigger-click").is(":visible") && $(".we-hashscroll-trigger-click").trigger("click"), $(destination).scrollToMe());
                                                } else e.preventDefault(), $("body").scrollToMe();
                                        }
                                    }
                                });
                            });
                        },
                        scrollToMe: function () {
                            var destination = $(this);
                            if (destination && destination.length) {
                                var destinationTop = destination.offset().top;
                                (destinationTop -= window.anchorAnimateOffset),
                                    $(window).trigger("we-hashscroll/scroll-start", window.anchorAnimateOffset, destinationTop),
                                    $("html:not(:animated),body:not(:animated)")
                                        .stop()
                                        .animate({ scrollTop: Math.floor(destinationTop) }, 600);
                            }
                            return this;
                        },
                    }),
                    $(window).on("load.hashscroll", function () {
                        window.location.hash &&
                            "" !== window.location.hash &&
                            $(window.location.hash) &&
                            1 === $(window.location.hash).length &&
                            setTimeout(function () {
                                $(window.location.hash).scrollToMe();
                            }, 200);
                    });
            },
            {},
        ],
        2: [
            function (require, module, exports) {
                "use strict";
                require("./js-hashscroll"), require("./we-helper"), require("./we-404"), require("./we-video"), require("./we-nav"), require("./we-icon");
            },
            { "./js-hashscroll": 1, "./we-404": 3, "./we-helper": 4, "./we-icon": 5, "./we-nav": 6, "./we-video": 7 },
        ],
        3: [
            function (require, module, exports) {
                "use strict";
                var $;
                ($ = jQuery)(window).on("load.404", function () {
                    var count = 10,
                        counter = null;
                    $("body").hasClass("error404") &&
                        (counter = setInterval(function () {
                            (count -= 1) > 0 ? $("[data-no-found-counter-index]").text(count) : (clearInterval(counter), (window.location.href = $("[data-no-found-href]").attr("href")));
                        }, 1e3));
                });
            },
            {},
        ],
        4: [
            function (require, module, exports) {
                "use strict";
                var $;
                ($ = jQuery).fn.isInViewport = function (tolerance) {
                    if (("number" != typeof tolerance && (tolerance = 0), $(this).length > 0)) {
                        var elementTop = $(this).offset().top - tolerance,
                            elementBottom = elementTop + $(this).height() + tolerance,
                            viewportTop = $(window).scrollTop(),
                            viewportBottom = viewportTop + $(window).height();
                        return elementBottom > viewportTop && elementTop < viewportBottom;
                    }
                    return !1;
                };
            },
            {},
        ],
        5: [
            function (require, module, exports) {
                "use strict";
                // var $;
                // ($ = jQuery)(document).ready(function () {
                //     window.UIkit.icon("[data-uk-icon]", {});
                // })
                    // $.get(localized.TemplatePath + "/assets/dist/images/map-pin.svg", function (response) {
                    //     if ((console.log(response.childNodes), 1 === response.childNodes.length)) {
                    //         var arrowImg = response.firstChild;
                    //         UIkit.icon.add({ location: arrowImg.outerHTML || new XMLSerializer().serializeToString(arrowImg) });
                    //     }
                    // });
            },
            {},
        ],
        6: [
            function (require, module, exports) {
                "use strict";
                var $;
                ($ = jQuery)(window).scroll(function () {
                    $(window).width() > 960 && ($(".navigation").toggleClass("navigation-sticky", $(window).scrollTop() > 1), $(".we-header").toggleClass("we-header-hide", $(window).scrollTop() > 1));
                }),
                    $(document).ready(function () {
                        $(window).trigger("scroll"),
                            $(window).width() < 960 && $("[data-uk-dropdown]").attr("data-uk-dropdown", "offset: 1; " + $("[data-uk-dropdown]").data("uk-dropdown")),
                            $("[data-navigation-toggler]").on("click", function () {
                                $("body").toggleClass("mobile-nav-open");
                            });
                    }),
                    $(window).on("load.anchoranimate resize.anchoranimate", function () {
                        (window.anchorAnimateOffset = 0),
                            $("#wpadminbar").each(function () {
                                $(this).outerHeight() && "fixed" === $(this).css("position") && (window.anchorAnimateOffset += $(this).outerHeight());
                            }),
                            "fixed" === $(".navigation").css("position") && (window.anchorAnimateOffset += $(".navigation").outerHeight());
                    }),
                    $('a[href*="#"]').anchorAnimate(),
                    setTimeout(function () {
                        $("body").removeClass("step-1");
                    }, 100),
                    setTimeout(function () {
                        $("body").removeClass("step-2");
                    }, 1200),
                    setTimeout(function () {
                        $("body").removeClass("step-3");
                    }, 800),
                    setTimeout(function () {
                        $("body").removeClass("step-4");
                    }, 1150),
                    setTimeout(function () {
                        $("body").removeClass("step-5");
                    }, 1500),
                    setTimeout(function () {
                        $("body").removeClass("step-6");
                    }, 1800),
                    setTimeout(function () {
                        $("body").removeClass("step-7");
                    }, 3e3);
            },
            {},
        ],
        7: [
            function (require, module, exports) {
                "use strict";
                var $;
                ($ = jQuery)(document).ready(function () {
                    $("[data-play-video]").on("click", function () {
                        var id = $(this).attr("data-play-video");
                        $('[data-video-id="' + id + '"]')
                            .get(0)
                            .play(),
                            $(this).addClass("faded"),
                            $(this).parent().find("[data-uk-img]").addClass("faded");
                    });
                });
            },
            {},
        ],
    },
    {},
    [2]
);
